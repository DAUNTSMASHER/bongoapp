# Database
DATABASE_URI=sqlite:///instance/project_ideas.db

# Docker: run container as your user to avoid root-owned files on host
# Linux/macOS: use `id -u` and `id -g` to get your UID/GID
# Windows (WSL2/Git Bash): run `id -u` and `id -g` in WSL or Git Bash
PUID=1000
PGID=1000
